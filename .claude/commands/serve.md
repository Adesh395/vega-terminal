Start the VEGA Terminal development server.

Run: `node serve.mjs`

- Serves the project root at http://localhost:3000
- Requires Node.js 18+
- If already running, do not start a second instance — check first with `curl -s http://localhost:3000`
